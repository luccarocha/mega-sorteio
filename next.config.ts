/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/mega-sorteio', // nome do seu repositório
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig